# Database Configuration for Olympian

# Database driver: mysql, postgres, or sqlite3
DB_DRIVER=mysql

# MySQL/PostgreSQL Configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=your_database
DB_USER=root
DB_PASS=

# For PostgreSQL, use port 5432
# DB_PORT=5432

# Note: For SQLite, use the --driver and --dsn flags instead
# Example: olympian migrate --driver sqlite3 --dsn ./database.db
